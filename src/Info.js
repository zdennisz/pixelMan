import React, { useState, useEffect, useRef } from 'react';
import {KeysInfo} from './KeysInfo'


export const Info=(props)=>{
    var keys=['w','s','a','d'];
    var altKeys=['up','down','left','right'];
    //<KeysInfo props={keys}/>
    //<KeysInfo props={altKeys}/>

//this component will hold the two sets of buttons plus the buttons for the audio player
//control set 1
//control set 2
//audio control
    return null;
}